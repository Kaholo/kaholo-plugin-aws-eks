const MISSING_OR_INCORRECT_CREDENTIALS_MESSAGE = "Missing or incorrect credentials - please select valid access and secret keys first";

const EKS_SERVICE_URL = "eks.amazonaws.com";

const DEFAULT_TOKEN_EXPIRE_TIME = 60;

module.exports = {
  MISSING_OR_INCORRECT_CREDENTIALS_MESSAGE,
  DEFAULT_TOKEN_EXPIRE_TIME,
  EKS_SERVICE_URL,
};
